# Core dependencies
requests
python-dateutil
numpy
pydantic

# Optional but recommended
jsonschema
PyYAML
rich
pathlib2

# Audio (optional)
soundfile

# Vector search (future feature)
faiss-cpu

# LLM clients (optional but safe to include)
ollama
